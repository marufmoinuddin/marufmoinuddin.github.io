/**
 * TOC generation for post/doc/research pages
 * This runs in addition to the inline TOC in main.js
 * to ensure TOCs are generated on dynamically loaded content.
 */
(function() {
  'use strict';
  document.addEventListener('DOMContentLoaded', function() {
    // The main.js already handles TOC generation.
    // This file exists as a fallback/separate module.
  });
})();
